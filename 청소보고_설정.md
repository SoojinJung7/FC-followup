# 🧹 청소 보고 봇 - 켜는 방법 (딱 4단계)

fc-followup 텔레그램 봇에 "사진 보내면 → 구역 버튼 → 보고방 자동 게시" 기능이 붙었어요.
아래 4단계만 하면 바로 씁니다. (예상 10분)

---

## 1단계. Supabase 에 기록표 만들기

1. Supabase 대시보드 → 이 프로젝트 → 왼쪽 **SQL Editor**
2. `supabase/cleaning.sql` 파일 내용을 그대로 붙여넣고 **RUN**
3. "Success" 뜨면 끝

## 2단계. 보고방 번호(chat id) 알아내기

이미 있는 청소 보고방에 봇을 초대한 뒤, 방에서 아무 말이나 한 마디 하고
아래 주소를 브라우저로 열어요 (`<토큰>` 자리에 봇 토큰):

```
https://api.telegram.org/bot<토큰>/getUpdates
```

`"chat":{"id":-100xxxxxxxxxx` 처럼 나오는 **마이너스 붙은 긴 숫자**가 방 번호예요.
(안 보이면 봇을 방에서 뺐다가 다시 넣고 한 번 더)

> 봇이 단톡방 글을 못 읽게 되어 있어도(기본값) 상관없어요.
> 봇은 방에 **올리기만** 하고, 사진은 봇과 1:1 대화로 보냅니다.

## 3단계. Vercel 환경변수 추가

Vercel → fc-followup 프로젝트 → Settings → Environment Variables 에 추가:

| 이름 | 값 | 설명 |
|---|---|---|
| `CLEANING_CHAT_ID` | `-100xxxxxxxxxx` | 2단계에서 알아낸 보고방 번호 |
| `TELEGRAM_WEBHOOK_SECRET` | 아무 긴 영문숫자 (예: 비밀번호 생성기로 32자) | 텔레그램만 봇 주소를 두드릴 수 있게 하는 자물쇠 |
| `CLEANING_USER_IDS` | 내 텔레그램 숫자 번호 (선택) | 비워두면 누구나 사용 가능. 봇에게 `/start` 치면 내 번호가 나와요 |

저장한 뒤 **Redeploy** 한 번 (Deployments → 최신 → ⋯ → Redeploy).

## 4단계. 텔레그램에게 "이 주소로 보내" 알려주기 (딱 한 번)

브라우저 주소창에 아래를 한 줄로 붙여넣고 엔터 (`<토큰>`, `<비밀>` 채우기):

```
https://api.telegram.org/bot<토큰>/setWebhook?url=https://fc-followup.vercel.app/api/telegram&secret_token=<비밀>&allowed_updates=["message","callback_query"]
```

`{"ok":true,"result":true,"description":"Webhook was set"}` 나오면 성공.

---

## 써보기

1. 텔레그램에서 봇과 1:1 대화 열기 → `/start`
2. 청소한 곳 사진 찍어서 보내기
3. 뜨는 버튼에서 **1층 로비 / 2층 샤워실 / 3층 락커** 중 하나 누르기
4. 보고방에 이렇게 올라감:

```
🧹 청소 완료 | 3층 락커
2026-09-06 (토) 14:32 · 수진
[사진]
```

- `/오늘` → 오늘 어디까지 했는지 확인
- 매일 **밤 9시** 봇이 보고방에 오늘 현황을 자동으로 올려요 (남은 곳 있으면 알려줌)
- 3곳 다 끝나면 봇이 바로 "🎉 전부 완료" 알려줌

## 자주 묻는 것

- **구역을 바꾸고 싶어요** → `lib/cleaning.ts` 맨 위 `AREAS` 목록만 고치면 돼요.
- **사진 여러 장을 한 번에 보냈어요** → 장마다 버튼이 하나씩 떠요. 한 장씩 보내는 게 깔끔해요.
- **저녁 요약 시간을 바꾸고 싶어요** → `vercel.json` 의 `0 12 * * *` (세계시 12시 = 한국 21시). 한국 20시면 `0 11 * * *`.
- **봇이 반응이 없어요** → `https://fc-followup.vercel.app/api/telegram` 을 브라우저로 열어 `"ok":true` 가 나오는지, 그리고 `getWebhookInfo` 로 웹훅이 걸렸는지 확인:
  `https://api.telegram.org/bot<토큰>/getWebhookInfo`
