# 📷 사진 업무보고 봇 - 켜는 방법

fc-followup 텔레그램 봇에 "사진 보내면 → AI가 보고문 작성 → [이대로 보고] 한 번 → 공용방 자동 게시" 기능이 붙었어요.
아래 5단계만 하면 바로 씁니다. (예상 15분)

> **0단계 (아직 봇이 없다면)**: 텔레그램에서 @BotFather → `/newbot` → 이름 정하기 → 받은 토큰을
> Vercel 환경변수 `TELEGRAM_BOT_TOKEN` 에 넣기. 이미 fc-followup 봇이 있으면 그 봇을 그대로 써요.

---

## 1단계. Supabase 에 기록표 만들기

1. Supabase 대시보드 → 이 프로젝트 → 왼쪽 **SQL Editor**
2. `supabase/reports.sql` 파일 내용을 그대로 붙여넣고 **RUN**
3. "Success" 뜨면 끝 (표 2개: reports, report_photos)

## 2단계. 공용방 번호(chat id) 알아내기

공용 보고방에 봇을 초대한 뒤, 방에서 아무 말이나 한 마디 하고 아래 주소를 브라우저로 열어요 (`<토큰>` 자리에 봇 토큰):

```
https://api.telegram.org/bot<토큰>/getUpdates
```

`"chat":{"id":-100xxxxxxxxxx` 처럼 나오는 **마이너스 붙은 긴 숫자**가 방 번호예요.
(안 보이면 봇을 방에서 뺐다가 다시 넣고 한 번 더)

## 3단계. AI 열쇠 만들기 (Anthropic)

1. https://console.anthropic.com → 로그인 → **Billing** 에서 결제수단 등록 (테스트라면 $10 정도 충전이면 충분)
2. **API Keys** → Create Key → 이름 "fc-followup" → 복사 (`sk-ant-...` 로 시작, 한 번만 보여줘요)

> 이 열쇠는 Claude Code 구독과 별개로 **쓴 만큼 과금**돼요. 사진 3장 보고 1건에 약 30~40원(Opus 기준).
> 봇에서 `/비용` 치면 이번 달 추정 비용을 바로 볼 수 있어요.

## 4단계. Vercel 환경변수 추가

Vercel → fc-followup 프로젝트 → Settings → Environment Variables:

| 이름 | 값 | 설명 |
|---|---|---|
| `CLEANING_CHAT_ID` | `-100xxxxxxxxxx` | 2단계에서 알아낸 공용방 번호 |
| `TELEGRAM_WEBHOOK_SECRET` | 아무 긴 영문숫자 (32자 정도) | 텔레그램만 봇 주소를 두드릴 수 있게 하는 자물쇠 |
| `ANTHROPIC_API_KEY` | `sk-ant-...` | 3단계 AI 열쇠. **비워두면 AI 없이 구역 버튼 방식으로 동작** |
| `AI_MODEL` | `claude-opus-5` (선택) | 테스트 후 `claude-sonnet-5` 로 바꾸면 비용 60% 절감 |
| `CLEANING_USER_IDS` | 텔레그램 숫자 번호들, 쉼표 구분 (선택) | 비워두면 누구나 사용 가능. `/start` 치면 내 번호가 나와요 |

저장한 뒤 **Redeploy** 한 번 (Deployments → 최신 → ⋯ → Redeploy).

## 5단계. 텔레그램에게 "이 주소로 보내" 알려주기 (딱 한 번)

브라우저 주소창에 아래를 한 줄로 붙여넣고 엔터 (`<토큰>`, `<비밀>` 채우기):

```
https://api.telegram.org/bot<토큰>/setWebhook?url=https://fc-followup.vercel.app/api/telegram&secret_token=<비밀>&allowed_updates=["message","callback_query"]
```

`{"ok":true,"result":true,"description":"Webhook was set"}` 나오면 성공.

---

## 써보기

1. 텔레그램에서 봇과 1:1 대화 열기 → `/start`
2. 일한 곳 사진을 찍어서 보내기 (텔레그램 카메라로 여러 장 연속 촬영 → 한 번에 전송 OK)
3. 2~3초 뒤 봇이 이렇게 물어봐요:

```
🤖 이렇게 보고할게요

3층 락커 청소 완료. 바닥 물기 제거, 쓰레기통 비움.
사진 3장 · 2026-09-06 (토) 14:32 · 확신 높음

[✅ 이대로 보고]
[✏️ 장소 고치기] [✖ 취소]
```

4. **[✅ 이대로 보고]** 한 번 → 공용방에 사진 3장 + 문장이 올라감. 끝.

- AI 가 헷갈릴 땐 질문 **딱 1개**만 해요 ("여기가 어디예요? [1층 로비] [3층 락커] …"). 누르면 바로 게시.
- `/오늘` → 오늘 어디까지 했는지 · `/비용` → 이번 달 AI 비용
- 매일 **밤 9시** 봇이 공용방에 오늘 현황 요약을 올려요
- 3곳 다 끝나면 봇이 바로 "🎉 전부 완료" 알려줌

### 💡 홈 화면에 바로가기 (앱처럼 쓰기)
텔레그램 → 봇 대화 → 오른쪽 위 ⋮ → **"홈 화면에 추가"**. 아이콘 누르면 바로 봇 대화가 열려요.

## 자주 묻는 것

- **구역을 바꾸고 싶어요** → `lib/cleaning.ts` 맨 위 `FLOORS` 목록(층별 25곳)만 고치면 돼요 (버튼·AI 지시문·요약에 자동 반영).
- **장소가 틀렸어요** → [✏️ 장소 고치기] → 층 → 장소 버튼. 목록에 없으면 [✍️ 직접 입력] 후 답장으로 장소 이름. 한 번 알려주면 다음부터 AI 가 후보로 기억해요.
- **AI 없이 쓰고 싶어요** → `ANTHROPIC_API_KEY` 를 비우면 예전처럼 구역 버튼이 떠요.
- **AI 모델 바꾸기** → Vercel 환경변수 `AI_MODEL` 을 `claude-sonnet-5` 로 → Redeploy.
- **저녁 요약 시간** → `vercel.json` 의 `0 12 * * *` (세계시 12시 = 한국 21시). 한국 20시면 `0 11 * * *`.
- **봇이 반응이 없어요** → `https://fc-followup.vercel.app/api/telegram` 을 브라우저로 열어 `"ok":true` 와 `"ai"` 값을 확인하고, 웹훅은 `https://api.telegram.org/bot<토큰>/getWebhookInfo` 로 확인.
- **사진을 "파일로" 보내면?** → 못 읽어요. 일반 사진으로 보내주세요.
